package pl.serweron.serweronLib.punishments;

/**
 * Defines whether the punishment applies globally across the network
 * or only locally to the current server.
 */
public enum PunishmentScope {
    GLOBAL,
    LOCAL
}
